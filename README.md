# Tiger City | الدليل التقني الشامل 🐅
**الإصدار: v1.0.0 | التحديث: يناير 2026 | البيئة: Production**

هذا المستند يعتبر المرجع التقني والتشغيلي الكامل لمشروع "Tiger City Web Portal". يغطي هذا الدليل كل سطر برمجي، كل قرار معماري، وكل أسرار النظام. تم تصميمه ليكون المصدر الوحيد للحقيقة (Source of Truth) للمطورين، الإداريين، واللاعبين المهتمين.

---

## 📑 فهرس المحتويات الشامل

1.  [نظرة عامة على المشروع (Project Abstract)](#1-نظرة-عامة-على-المشروع)
2.  [المتطلبات والبيئة (Prerequisites)](#2-المتطلبات-والبيئة)
3.  [دليل التثبيت والتشغيل (Installation & Setup)](#3-دليل-التثبيت-والتشغيل)
4.  [الهيكلية المعمارية للنظام (System Architecture)](#4-الهيكلية-المعمارية-للنظام)
5.  [توثيق المكونات (Component Documentation)](#5-توثيق-المكونات)
6.  [نظام الأسرار (The Hidden Layer)](#6-نظام-الأسرار-the-hidden-layer)
7.  [المرجع البرمجي العميق (Deep Code Reference)](#7-المرجع-البرمجي-العميق) **(جديد)**
8.  [نظام التصميم (Design System & CSS)](#8-نظام-التصميم-design-system--css)
9.  [النشر المتقدم و CI/CD (Advanced Deployment)](#9-النشر-المتقدم-و-cicd) **(جديد)**
10. [الأمان والأداء (Security & Performance)](#10-الأمان-والأداء) **(جديد)**
11. [معايير المساهمة (Contribution Standards)](#11-معايير-المساهمة) **(جديد)**
12. [استكشاف الأخطاء وإصلاحها (Troubleshooting)](#12-استكشاف-الأخطاء-وإصلاحها)

---

## 1. نظرة عامة على المشروع

مشروع **Tiger City** هو تطبيق صفحة واحدة (Single Page Application - SPA) متقدم، تم بناؤه باستخدام **React 19** ومجمع الحزم **Vite**. الغرض الأساسي منه هو توفير واجهة ويب غنية وتفاعلية لمجتمع لعبة روبلوكس، مع التركيز على الأداء العالي، الجماليات البصرية الحديثة، وتجربة المستخدم السلسة.

**التقنيات الأساسية:**
-   **Framework:** React 19 (Functional Components + Hooks)
-   **Build Tool:** Vite 7.2
-   **Routing:** React Router DOM v7 (HashRouter implementation)
-   **Styling:** Pure CSS3 with CSS Variables (No external UI libraries)
-   **Icons:** Lucide React
-   **Language:** JavaScript (ES6+)

---

## 2. المتطلبات والبيئة

قبل البدء في التطوير، يجب التأكد من توفر البيئة التالية:

| المتطلب | الإصدار الأدنى | الملاحظات |
| :--- | :--- | :--- |
| **Node.js** | v18.0.0 | مطلوب لدعم ميزات Vite الحديثة |
| **npm** | v9.0.0 | مدير الحزم الافتراضي |
| **Git** | v2.30+ | لإدارة النسخ والتحكم بالمصدر |
| **Browser** | Chrome/Edge 90+ | لدعم CSS Grid و Backdrop-filter |

---

## 3. دليل التثبيت والتشغيل

### 3.1 الاستنساخ (Cloning)
```bash
git clone https://github.com/your-username/tiger-city-web.git
cd tiger
```

### 3.2 التثبيت (Installation)
يقوم هذا الأمر بقراءة `package.json` وسحب جميع الاعتماديات وتخزينها في `node_modules`.
```bash
npm install
# أو باستخدام yarn
yarn install
```

### 3.3 بيئة التطوير (Development)
يقوم هذا الأمر بتشغيل سيرفر Vite المحلي مع ميزة Hot Module Replacement (HMR).
```bash
npm run dev
```
> **ملاحظة:** سيعمل السيرفر افتراضياً على المنفذ `5173`. يمكن تغييره في `vite.config.js`.

### 3.4 بناء النسخة النهائية (Production Build)
```bash
npm run build
```
سيقوم بإنشاء مجلد `dist` يحتوي على ملفات HTML/CSS/JS المضغوطة والجاهزة للرفع.

---

## 4. الهيكلية المعمارية للنظام

### Routing Strategy
تم استخدام **`HashRouter`** بدلاً من `BrowserRouter`.
*   **السبب:** لضمان عمل الموقع بشكل صحيح عند استضافته على استضافات ثابتة (مثل GitHub Pages) التي لا تدعم إعادة توجيه الطلبات (SPA Rewrites - History API Fallback).
*   **المسارات:**
    *   `/` -> Home
    *   `/updates` -> Updates
    *   `/secret` -> **مسار مخفي**

### State Management & Context
لا يستخدم المشروع مكتبات ثقيلة مثل Redux. بدلاً من ذلك، نستخدم **React Context Pattern** لإدارة البيانات العالمية.
*   **`AvatarContext`**: هو المخزن المركزي لصور اللاعبين.
    *   يقوم بتخزين الصور في كائن (Object) حيث المفتاح هو `userId` والقيمة هي `imageUrl`.
    *   يمنع تكرار طلبات الشبكة لنفس اللاعب (Deduplication).

### API Integration Layer
*   **Endpoints**: يتم الاتصال بـ Roblox API عبر وسيط (Proxy) لتجاوز مشاكل الـ CORS.
*   **RoProxy**: `https://thumbnails.roproxy.com/v1/users/avatar-headshot`
*   **طريقة الاسترجاع**: `fetch` API القياسية مع `async/await`.

---

## 5. توثيق المكونات

### Core Layout
*   **`App.jsx`**: الحاوية الرئيسية. تحتوي على عنصر `<Cursor />` الذي يتبع حركة الماوس ويغير شكله عند المرور فوق الروابط (تفاعل Hover). يتم استخدام `useState` لتتبع إحداثيات `clientX/Y` وتحديث موقع الـ Cursor.
*   **`Navbar.jsx`**: يستخدم `NavLink` من مكتبة Router ليضيف كلاس `.active` تلقائياً للرابط الحالي، مما يسمح بتمييز الصفحة النشطة بصرياً.

### الصفحة الرئيسية (Home Logic)
مسار الملف: `src/pages/Home.jsx`
*   **الحالة (State):**
    *   `playerCount`: عدد اللاعبين الحالي (Number).
    *   `mapStatus`: حالة السيرفر (Enum: 'شغال', 'صيانة', 'موقف').
*   **الدورة الحياتية (Lifecycle):**
    *   يستخدم `useEffect` لتشغيل `setInterval` كل 10 ثوانٍ.
    *   يستخدم `clearInterval` في دالة الـ cleanup.

### نظام الجريدة الصحفية (Updates System)
مسار الملف: `src/pages/Updates.jsx`
*   يعتمد كلياً على ملف بيانات منفصل (`src/data/dailyContent.js`).
*   **الميزات الجمالية:** يستخدم أيقونات متغيرة حسب نوع الخبر (نار للحوادث، دولار للاقتصاد) عبر دالة `getIconForCategory`.

### القوائم السوداء (Wanted Feature)
مسار الملف: `src/components/MostWanted.jsx`
*   يقرأ مصفوفة `WANTED_LIST`.
*   لكل عنصر، يقوم باستدعاء `fetchAvatars` من الـ Context.
*   **Easter Egg Trigger**:
    *   يوجد عداد مخفي (`secretClicks`).
    *   عند النقر على أيقونة "Skull"، يتم زيادة العداد.
    *   الشرط: `if (newCount >= 10) navigate('/secret')`.

---

## 6. نظام الأسرار (The Hidden Layer)

الجزء الأكثر تعقيداً في المشروع هو صفحة **Dark Web** (`src/pages/Secret.jsx`).

### Matrix Animation Engine
يستخدم عنصر `<canvas>` HTML5 ورسم مباشر عبر JavaScript.
*   **الخوارزمية:**
    1.  إنشاء مصفوفة `drops` بعدد الأعمدة الممكنة في الشاشة.
    2.  في كل إطار (Frame)، يتم رسم مستطيل شفاف أسود (لعمل تأثير الذيل المتلاشي).
    3.  يتم رسم حرف عشوائي باللون الأخضر في موقع كل "قطرة".
    4.  يتم تحريك القطرة للأسفل بمقدار الخط، وإعادتها للأعلى بشكل عشوائي عند تجاوز ارتفاع الشاشة.

### Terminal Command Processor
نظام محاكاة سطر الأوامر (CLI):
*   يستمع لحدث `onKeyDown` (مفتاح Enter).
*   يقوم بتنظيف المدخلات (`trim().toLowerCase()`).
*   **قائمة الأوامر المعرفة مسبقاً:**
    | الأمر | الوظيفة |
    | :--- | :--- |
    | `help` | عرض المساعدة |
    | `gangs` | فك تشفير البيانات |
    | `leaks` | عرض التسريبات |
    | `clear` | مسح الشاشة |
    | `exit` | العودة للرئيسية |

---

## 7. المرجع البرمجي العميق

في هذا القسم، نستعرض المقتطفات البرمجية الجوهرية التي تشغّل النظام.

### منطق جلب الصور (Avatar Deduplication)
في `src/context/AvatarContext.jsx`:
```javascript
// نقوم بتصفية الآيديات قبل إرسال الطلب لتجنب طلب صور موجودة مسبقاً
const fetchAvatars = useCallback(async (userIds) => {
    // 1. تحويل المدخلات لمصفوفة
    const ids = Array.isArray(userIds) ? userIds : [userIds];
    
    // 2. البحث عن الآيديات غير الموجودة في الـ Cache
    const missingIds = ids.filter(id => !avatars[id]);

    if (missingIds.length === 0) return; // لا تفعل شيئاً إذا كان الكل موجوداً

    // 3. بناء رابط الـ Proxy
    const idsParam = missingIds.join(',');
    const targetUrl = `https://thumbnails.roproxy.com/v1/users/avatar-headshot?userIds=${idsParam}&size=420x420&format=Png&isCircular=false`;
    
    // 4. جلب البيانات وتحديث الـ UseState
    // ...
}, [avatars]);
```

### خوارزمية كسر الحماية (Secret Trigger)
في `src/components/MostWanted.jsx`:
```javascript
const handleSkullClick = () => {
    const newCount = secretClicks + 1;
    setSecretClicks(newCount);
    
    // عند الوصول للنقرة العاشرة، فعّل الانتقال
    if (newCount >= 10) {
        navigate('/secret'); 
        setSecretClicks(0); 
    }
};
```

---

## 8. نظام التصميم (Design System & CSS)

### Global Tokens
في ملف `src/index.css`:
```css
:root {
  --color-bg: #0a0a12;        /* خلفية داكنة جداً */
  --color-primary: #0056b3;   /* أزرق داكن */
  --color-accent: #00c3ff;    /* سماوي ساطع (Cyan) */
  --font-main: 'Cairo', sans-serif; /* الخط العربي */
  --radius-lg: 20px;          
}
```

### Glassmorphism Strategy
لتحقيق مظهر الزجاج الشفاف، نستخدم كلاس `.glass-panel`:
```css
.class-panel {
  background: rgba(20, 20, 30, 0.6);
  backdrop-filter: blur(12px); /* تأثير الضباب الخلفي */
  border: 1px solid rgba(255, 255, 255, 0.05); /* حدود شبه شفافة */
}
```

---

## 9. النشر المتقدم و CI/CD

لضمان عمل الموقع بكفاءة عالية على مختلف المنصات، اتبع التعليمات التالية.

### GitHub Pages (Automated Workflow)
لأتمتة عملية الرفع، يمكنك استخدام GitHub Actions. قم بإنشاء ملف `.github/workflows/deploy.yml`:

```yaml
name: Deploy to GitHub Pages
on:
  push:
    branches: [ main ]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Install and Build
        run: |
          npm install
          npm run build
      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist
```

### Docker (للبيئات المعزولة)
إذا كنت ترغب بتشغيل الموقع في حاوية:
```dockerfile
# Stage 1: Build
FROM node:18-alpine as builder
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

# Stage 2: Serve
FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

---

## 10. الأمان والأداء

### استراتيجية CORS Proxy
نستخدم `roproxy.com` لأنه وسيط مفتوح المصدر ومجاني.
*   **المخاطر:** الاعتماد على طرف ثالث يعني احتمالية توقفه.
*   **الحل المستقبلي:** بناء Backend خاص (Node/Express) ليكون هو الوسيط الرسمي بدلاً من الاعتماد على خدمات عامة.

### التحميل الكسول (Lazy Loading)
يقوم Vite تلقائياً بتقسيم الكود (Code Splitting). لاحقاُ، يمكن تحسين الأداء باستخدام `React.lazy` للصفحات الكبيرة:
```javascript
const Secret = React.lazy(() => import('./pages/Secret'));
```
هذا يضمن أن كود صفحة "Secret" لا يتم تحميله إلا عند دخول المستخدم لها، مما يسرع تحميل الصفحة الرئيسية.

### تحسين الأصول (Assets)
*   الخطوط المستخدمة (`Cairo`) يتم تحميلها من Google Fonts عبر CDN لتقليل استهلاك الباندويث للسيرفر.
*   الأيقونات (`lucide-react`) هي SVG وتدعم Tree-shaking، مما يعني أن الأيقونات غير المستخدمة لا تدخل في ملف البناء النهائي.

---

## 11. معايير المساهمة

للحفاظ على جودة الكود، يرجى اتباع المعايير التالية عند إرسال Pull Requests:

### Git Flow
*   **main**: الفرع المستقر (Production Ready).
*   **dev**: فرع التطوير (التجارب).
*   **feature/XYZ**: فرع لميزة جديدة (مثل: feature/login-system).
*   **fix/XYZ**: فرع لإصلاح خطأ (مثل: fix/nav-bug).

### رسائل الالتزام (Commit Messages)
نتبع نظام **Conventional Commits**:
*   `feat: add new map vote system` (ميزة جديدة)
*   `fix: resolve cursor overlapping issue` (إصلاح خطأ)
*   `style: update header gradient color` (تعديل تصميم فقط)
*   `docs: update readme instructions` (تعديل توثيق)

---

## 12. استكشاف الأخطاء وإصلاحها

### مشكلة: الصور لا تظهر (Roblox Avatars)
*   **السبب المحتمل:** خدمة `RoProxy` قد تكون متوقفة مؤقتاً أو محجوبة.
*   **الحل:** تفقد في الكونسول (F12) إذا كان هناك أخطاء `429 Too Many Requests`. النظام مصمم ليعمل حتى بدون صور (يظهر Placeholder).

### مشكلة: الصفحة بيضاء عند الفتح (White Screen)
*   **السبب:** خطأ في المسار الأساسي (Base Path).
*   **الحل:** تأكد أن `base` في `vite.config.js` يطابق اسم المستودع في GitHub Pages.

---
**تم إعداد هذا التوثيق ليكون المصدر النهائي للحقيقة لفريق تطوير Tiger City.**
*تمت كتابته بعناية فائقة لضمان استمرارية المشروع وسهولة تسليمه لأي مطور جديد.*
